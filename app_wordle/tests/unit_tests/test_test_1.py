def test_test():
    """
       AVEC l'application flask
       QUAND on lance les tests avec la commande "pytest"
       ALORS on vérifie que les tests s'exécutent correctement
       """
    assert True
